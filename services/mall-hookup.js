const { ipv4Converter } = require("../utilities/string-helper");
const {models} = require('../database');

const getMallHookupData = async (req) => {

    try {




    } catch (error) {
        console.log(error);
    }

}


const uploadMallHookupDataBulk = async (data) => {





}

module.exports = {
    getMallHookupData,
    uploadMallHookupDataBulk
}